if [ "$RUN_DOCKER_CMD_AS_ROOT" = "false" ]
then
    DOCKER_SUDO=sudo
else
    DOCKER_SUDO=
fi

function getNetWorkName()
{
    echo "$(docker network ls | sed 's/[ ]\+/ /g' | cut -d' ' -f2 | grep ${app_name}_)"
}


